import React,{useEffect,useMemo,useRef,useState} from 'https://esm.sh/react@18.3.1';
import{createRoot}from'https://esm.sh/react-dom@18.3.1/client';
import{Renderer,Stave,StaveNote,Voice,Formatter}from'https://cdn.jsdelivr.net/npm/vexflow@5.0.0/build/esm/entry/vexflow.js';
const h=React.createElement;
const TICKS={whole:1920,half:960,quarter:480,eighth:240,sixteenth:120};
const DURATIONS=[['whole','Semibreve','𝅝'],['half','Mínima','𝅗𝅥'],['quarter','Semínima','♩'],['eighth','Colcheia','♪'],['sixteenth','Semicolcheia','𝅘𝅥𝅮']];
const PITCHES=['c/5','b/4','a/4','g/4','f/4','e/4','d/4','c/4','b/3'];
function createScore(){return{schemaVersion:1,id:crypto.randomUUID(),metadata:{title:'',composer:''},parts:[{id:'part-01',name:'Piano',staves:[{id:'staff-01',clef:{sign:'G',line:2,octaveChange:0},measures:[{number:1,timeSignature:{numerator:4,denominator:4,grouping:[2,2]},voices:[{id:'voice-1',index:1,events:[]}]}]}]}]};}
function Engine(score){const copy=()=>structuredClone(score);return{insertNote(pitch,duration){const next=copy(),events=next.parts[0].staves[0].measures[0].voices[0].events,used=events.reduce((s,e)=>s+TICKS[e.duration.base],0),amount=TICKS[duration];if(used+amount>1920)return{ok:false,reason:'Este compasso não comporta mais essa duração.'};events.push({id:crypto.randomUUID(),type:'note',position:used,duration:{base:duration,dots:0,tuplet:null},pitch:pitchModel(pitch)});return{ok:true,score:next};},deleteLast(){const next=copy();next.parts[0].staves[0].measures[0].voices[0].events.pop();return next;}};}
function pitchModel(key){const [step,octave]=key.split('/');return{step:step.toUpperCase(),alter:0,octave:Number(octave)};}
function vexEvent(e){const key=e.pitch.step.toLowerCase()+'/'+e.pitch.octave;const d={whole:'w',half:'h',quarter:'q',eighth:'8',sixteenth:'16'}[e.duration.base];return new StaveNote({keys:[key],duration:d});}
function Staff({score,onInsert,duration}){const ref=useRef(null);const geometry=useRef(null);useEffect(()=>{const host=ref.current;if(!host)return;host.innerHTML='';const width=Math.max(300,host.clientWidth||300),height=230,left=30,right=20,staveWidth=Math.max(250,width-left-right);try{const renderer=new Renderer(host,Renderer.Backends.SVG);renderer.resize(width,height);const ctx=renderer.getContext();const stave=new Stave(left,68,staveWidth);stave.addClef('treble').addTimeSignature('4/4');stave.setContext(ctx).draw();geometry.current={left,top:stave.getYForLine(0),bottom:stave.getYForLine(4),right:left+staveWidth};const events=score.parts[0].staves[0].measures[0].voices[0].events;if(events.length){const notes=events.map(vexEvent);const voice=new Voice({numBeats:4,beatValue:4});voice.setMode(Voice.Mode.SOFT);voice.addTickables(notes);new Formatter().joinVoices([voice]).format([voice],Math.max(170,staveWidth-120));voice.draw(ctx,stave);}}catch(err){console.error('Falha ao renderizar pauta',err);} },[score]);
const handlePointer=(ev)=>{const g=geometry.current;if(!g)return;const r=ref.current?.getBoundingClientRect();if(!r)return;const x=ev.clientX-r.left,y=ev.clientY-r.top;if(x<g.left||x>g.right)return;const halfStep=5;const topLedger=g.top-20;const bottomLedger=g.bottom+30;if(y<topLedger||y>bottomLedger)return;const diatonic=Math.round((y-g.top)/halfStep);const pitchByPosition={[-4]:'F5',[-3]:'E5',[-2]:'D5',[-1]:'C5',[0]:'B4',[1]:'A4',[2]:'G4',[3]:'F4',[4]:'E4',[5]:'D4',[6]:'C4',[7]:'B3',[8]:'A3',[9]:'G3',[10]:'F3'};const nearest=Math.max(-4,Math.min(8,diatonic));const pitch=pitchByPosition[nearest];if(!pitch)return;onInsert(pitch.toLowerCase().replace(/([a-g])(\d)/,'$1/$2'),duration);};
return h('div',{className:'staff-wrap'},h('div',{ref,className:'staff',onPointerDown:handlePointer,role:'application','aria-label':'Pauta musical. Toque ou clique na altura desejada para inserir a nota.'}),h('div',{className:'staff-hint'},'Clique/toque exatamente na altura desejada da pauta. A nota é encaixada na linha ou espaço mais próximo.'));}
function App(){const[score,setScore]=useState(()=>{try{return JSON.parse(localStorage.getItem('music-note-score'))||createScore()}catch{return createScore()}}),[duration,setDuration]=useState('quarter'),[saved,setSaved]=useState(false),[message,setMessage]=useState('');const events=score.parts[0].staves[0].measures[0].voices[0].events,used=events.reduce((s,e)=>s+TICKS[e.duration.base],0),engine=useMemo(()=>Engine(score),[score]);
const insert=(p,d)=>{const r=engine.insertNote(p,d);if(r.ok){setScore(r.score);setMessage('Nota inserida');}else setMessage(r.reason);};
const save=()=>{localStorage.setItem('music-note-score',JSON.stringify(score));setSaved(true);setMessage('Partitura salva neste dispositivo');setTimeout(()=>setSaved(false),1600);};
const undo=()=>{if(events.length){setScore(Engine(score).deleteLast());setMessage('Última nota desfeita');}};
const fresh=()=>{setScore(createScore());setMessage('Nova partitura');};
useEffect(()=>{const f=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'){e.preventDefault();undo();}};window.addEventListener('keydown',f);return()=>window.removeEventListener('keydown',f)},[score,events.length]);
return h('main',null,
 h('header',{className:'topbar'},h('div',{className:'brand'},h('span',{className:'brand-mark'},'♫'),h('div',null,h('strong',null,'Bloco de Notas Musical'),h('small',null,'Abra · escreva · ouça · salve'))),h('div',{className:'actions'},h('button',{onClick:fresh},'Nova'),h('button',{className:'primary',onClick:save},saved?'Salvo ✓':'Salvar'))),
 h('section',{className:'workspace'},
 h('div',{className:'toolbar'},h('div',{className:'tool-group'},h('span',{className:'label'},'Duração'),...DURATIONS.map(d=>h('button',{key:d[0],type:'button',className:duration===d[0]?'tool active':'tool',onClick:()=>{setDuration(d[0]);setMessage(d[1]+' selecionada');},title:'Selecionar '+d[1],'aria-pressed':duration===d[0]},h('span',null,d[2]),h('small',null,d[1])))),h('div',{className:'meter'},h('span',null,'4/4'),h('small',null,used>=1920?'Compasso completo':((1920-used)/480)+' tempos disponíveis')),h('button',{className:'undo',onClick:undo,disabled:!events.length},'↶ Desfazer')),
 h('div',{className:'mobile-selected'},'Duração: ',h('b',null,DURATIONS.find(d=>d[0]===duration)?.[1]),' · ',h('span',null,'toque na pauta para inserir')),
 h('div',{className:'sheet-card'},h('div',{className:'sheet-header'},h('div',null,h('span',{className:'eyebrow'},'PARTITURA'),h('h1',null,score.metadata.title||'Sem título')),h('div',{className:'voice-status'},'1 pauta · 1 voz · Sol')),h(Staff,{score,onInsert:insert,duration}),h('div',{className:'statusline'},h('span',null,h('b',null,events.length),events.length===1?' nota':' notas'),h('span',null,'Compasso 1 · '+used/480+' / 4 tempos'),h('span',{className:'ready'},'● '+(message||'Pronto para escrever'))))),
 h('footer',null,'Protótipo inicial · Motor musical separado da renderização · Dados salvos localmente'));}
createRoot(document.getElementById('root')).render(h(App));